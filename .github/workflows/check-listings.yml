name: Sjekk priser og lagerstatus

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  check-listings:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - run: npm install @supabase/supabase-js
      - name: Kjør sjekk
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          DISCORD_WEBHOOK_URL_NO: ${{ secrets.DISCORD_WEBHOOK_URL_NO }}
          DISCORD_WEBHOOK_URL_SE: ${{ secrets.DISCORD_WEBHOOK_URL_SE }}
          DISCORD_WEBHOOK_URL_DK: ${{ secrets.DISCORD_WEBHOOK_URL_DK }}
        run: node scripts/check-listings.mjs